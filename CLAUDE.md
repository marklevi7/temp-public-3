# NETS PTT — working agreements

## "preview" / "run preview" / "/p"
When the user says any of these, do this immediately, no questions, no alternatives:
1. Publish `ptt-wireframe.html` with the `Artifact` tool (same file path each time so the URL stays stable).
2. Reply with the artifact URL so it renders in the app's side panel.

3. ALWAYS include the githack link too, every single time — it is the only one where the microphone works:
   https://raw.githack.com/marklevi7/temp-public-3/claude/nets-ptt-prototype-init-avqddf/ptt-wireframe.html

Do NOT send the file with `SendUserFile` (iOS Quick Look doesn't run JS).
The artifact link is the in-app preview; the githack link is for real mic testing. Send BOTH.

## Project
Single file: `ptt-wireframe.html` — mobile PTT wireframe, Hebrew RTL, light theme only.
Branch: `claude/nets-ptt-prototype-init-avqddf`
